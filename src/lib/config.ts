// Theme configuration
// Set THEME_PICKER to false to lock the app to DEFAULT_THEME and hide the picker.
//
// NOTE: src/app.html restores the stored theme before paint to avoid a flash, and being
// static HTML it cannot import this module. It carries its own THEME_PICKER constant that
// must be kept in sync with the one below.
export const THEME_PICKER = true;
export const DEFAULT_THEME = 'mistahuman-theme';
